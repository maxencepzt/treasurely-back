# Serveur unique de l'image Dockerfile.vercel : FrankenPHP écoute sur le port que
# Vercel injecte dans PORT (80 par défaut) et sert Symfony depuis public/.
:{$PORT:80} {
    root * /app/public
    encode zstd gzip
    php_server
}
