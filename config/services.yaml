# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones

    # Sessions de la façade concepteur et de l'administration en base : en production le
    # conteneur tourne en plusieurs instances, et un fichier de session n'existe que sur celle
    # qui l'a écrit. Le gestionnaire partage la connexion PDO de Doctrine, d'où le verrou
    # consultatif : le verrou transactionnel (défaut) ouvrirait une transaction que les flush
    # de Doctrine ne pourraient plus imbriquer.
    Symfony\Component\HttpFoundation\Session\Storage\Handler\PdoSessionHandler:
        arguments:
            - '@=service("doctrine.dbal.default_connection").getNativeConnection()'
            - { lock_mode: !php/const Symfony\Component\HttpFoundation\Session\Storage\Handler\PdoSessionHandler::LOCK_ADVISORY }

    App\ApiPlatform\InheritedResourceMetadataFactory:
        decorates: api_platform.metadata.resource.metadata_collection_factory
        # Sous le décorateur de cache (-10) : la réponse héritée est celle, déjà mise en cache, du parent.
        decoration_priority: -20
