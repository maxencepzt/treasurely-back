controllers:
    resource:
        path: ../src/Controller/
        namespace: App\Controller
    type: attribute
api_login:
  path: /api/auth
  methods: ['POST']
api_refresh_token:
  path: /api/token/refresh
api_token_invalidate:
  path: /api/token/invalidate
sso_logout:
  path: /sso/logout
